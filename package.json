{
  "name": "hello-world-addon",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "build:node": "node-gyp clean && node-gyp configure && node-gyp build",
    "build:electron": "node-gyp clean && node-gyp configure && node-gyp rebuild --runtime=electron --target=5.0.13 --dist-url=https://atom.io/download/electron",
    "test:node": "node index.js",
    "test:electron": "electron ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "electron": "^9.1.0"
  }
}
